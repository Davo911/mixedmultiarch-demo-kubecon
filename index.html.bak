<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Multi-Arch Demo</title>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=IBM+Plex+Sans:wght@300;400;600&display=swap"
      rel="stylesheet"
    />
    <style>
      :root {
        color-scheme: light;
      }

      * {
        box-sizing: border-box;
      }

      body {
        margin: 0;
        min-height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 3rem 1.5rem;
        font-family: 'IBM Plex Sans', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
        background: linear-gradient(160deg, #f5f6f8 0%, #e2e3e7 45%, #c9cace 100%);
        color: #1f2329;
      }

      main {
        text-align: center;
        display: flex;
        flex-direction: column;
        align-items: center;
        gap: 1.75rem;
        width: min(560px, 100%);
      }

      h1 {
        margin: 0;
        font-size: clamp(2.6rem, 6vw, 3.6rem);
        font-weight: 600;
      }

      p {
        margin: 0;
        font-size: clamp(1.05rem, 2.5vw, 1.25rem);
        color: rgba(31, 35, 41, 0.72);
        line-height: 1.6;
      }

      .actions {
        display: flex;
        flex-wrap: wrap;
        justify-content: center;
        gap: 1rem;
        margin-top: 1rem;
      }

      .button {
        appearance: none;
        border: none;
        border-radius: 999px;
        padding: 0.9rem 2.4rem;
        font-size: 1.1rem;
        font-weight: 600;
        letter-spacing: 0.04em;
        text-transform: uppercase;
        color: #ffffff;
        cursor: pointer;
        transition: transform 0.2s ease, box-shadow 0.2s ease;
        background: linear-gradient(120deg, #4b5563, #1f2937);
        box-shadow: 0 18px 35px rgba(31, 41, 55, 0.28);
      }

      .button:hover,
      .button:focus-visible {
        transform: translateY(-2px);
        box-shadow: 0 20px 40px rgba(31, 41, 55, 0.32);
      }

      .button:active {
        transform: translateY(0);
        box-shadow: 0 12px 25px rgba(31, 41, 55, 0.24);
      }

      footer {
        font-size: 0.95rem;
        color: rgba(31, 35, 41, 0.55);
        letter-spacing: 0.08em;
        text-transform: uppercase;
      }
    </style>
  </head>
  <body>
    <main>
      <h1>Choose Your Architecture</h1>
      <p>
        Demonstrating how KubeVirt schedules workloads exactly where they belong. Pick the experience designed
        for your application profile.
      </p>
      <div class="actions">
        <button class="button" type="button">Your CUDA Workload</button>
        <button class="button" type="button">Your COBOL Transaction</button>
      </div>
      <footer>Multi-Architecture Demo • KubeVirt</footer>
    </main>
  </body>
</html>
